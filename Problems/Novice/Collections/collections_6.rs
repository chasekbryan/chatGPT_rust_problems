// collections_6.rs
// Type: Collections
// Difficulty: Novice (Level 1)
//
// Lesson:
// You can remove elements from a `Vec` using the `pop` method, which removes and returns the last element.
//
// Instructions:
// 1. Create a `Vec` named `stack` with the values `[1, 2, 3]`.
// 2. Pop the last element and print it.
// 3. Print the updated vector.
//
// Hints:
// - `.pop()` returns an `Option`, so you may need to use `match` or assume it’s `Some(value)` here for simplicity.

fn main() {
    // TODO: Pop the last element and print both the popped value and the updated vector
}
