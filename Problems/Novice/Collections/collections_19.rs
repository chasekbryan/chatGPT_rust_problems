// collections_19.rs
// Type: Collections
// Difficulty: Novice (Level 1)
//
// Lesson:
// You can find the index of an element in a vector using `.iter().position()` which returns an `Option`.
//
// Instructions:
// 1. Create a `Vec` named `numbers` with the values `[10, 20, 30, 40, 50]`.
// 2. Find the index of the value `30`.
// 3. Print the index if found, or print a message if not found.
//
// Hints:
// - Use `.iter().position(|&x| x == 30)` to find the position of `30`.
// - Handle the `Option` result with `match`.

fn main() {
    // TODO: Find the index of 30 and print the result
}
