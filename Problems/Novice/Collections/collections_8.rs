// collections_8.rs
// Type: Collections
// Difficulty: Novice (Level 1)
//
// Lesson:
// You can remove an element at a specific index using `.remove(index)`, which also returns the removed value.
//
// Instructions:
// 1. Create a `Vec` named `letters` with the values `['a', 'b', 'c', 'd']`.
// 2. Remove the element at index `2`.
// 3. Print the removed element and the updated vector.
//
// Hints:
// - `.remove(index)` returns the removed item.
// - Use `println!` to print both the removed item and the vector.

fn main() {
    // TODO: Remove the element at index 2 and print everything
}
