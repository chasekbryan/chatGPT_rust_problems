// collections_5.rs
// Type: Collections
// Difficulty: Novice (Level 1)
//
// Lesson:
// You can iterate over a `Vec` using a `for` loop to access each element.
//
// Instructions:
// 1. Create a `Vec` named `animals` with the values `["dog", "cat", "bird"]`.
// 2. Use a `for` loop to print each animal.
//
// Hints:
// - Use `for item in vec.iter()` to loop over elements without taking ownership.
// - Print inside the loop with `println!`.

fn main() {
    // TODO: Loop over the animals vector and print each animal
}
