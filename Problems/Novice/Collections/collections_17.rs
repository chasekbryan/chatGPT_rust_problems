// collections_17.rs
// Type: Collections
// Difficulty: Novice (Level 1)
//
// Lesson:
// You can remove all elements from a vector that match a condition using `.retain()`.
//
// Instructions:
// 1. Create a `Vec` named `values` with the numbers `[10, 15, 20, 25, 30]`.
// 2. Use `.retain()` to remove all values greater than 20.
// 3. Print the resulting vector.
//
// Hints:
// - `.retain()` accepts a closure that returns `true` to keep an element or `false` to remove it.

fn main() {
    // TODO: Retain elements less than or equal to 20 and print the result
}
