// collections_12.rs
// Type: Collections
// Difficulty: Novice (Level 1)
//
// Lesson:
// You can concatenate two vectors by extending one with the elements of another using `.extend()`.
//
// Instructions:
// 1. Create two `Vec`s: `a = [1, 2, 3]` and `b = [4, 5, 6]`.
// 2. Extend `a` with `b`.
// 3. Print the resulting `a` vector.
//
// Hints:
// - `.extend()` takes anything that implements `IntoIterator`, including another vector.
// - After extending, `a` will contain all elements.

fn main() {
    // TODO: Extend the first vector with the second and print the result
}
