// tuples_and_arrays_5.rs
// Type: Tuples and Arrays
// Difficulty: Novice (Level 1)
//
// Lesson:
// Arrays have a fixed length known at compile time. You can find the length of an array with `.len()`.
//
// Instructions:
// 1. Create an array named `letters` with five characters: `['a', 'b', 'c', 'd', 'e']`.
// 2. Print the length of the array.
//
// Hints:
// - Arrays use square brackets `[]` and separate elements with commas.
// - Use `.len()` to get the number of elements.

fn main() {
    // TODO: Create the array and print its length
}
