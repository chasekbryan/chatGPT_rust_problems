// tuples_and_arrays_18.rs
// Type: Tuples and Arrays
// Difficulty: Novice (Level 1)
//
// Lesson:
// Tuple structs are a way to create custom types that behave like tuples but have named meanings.
//
// Instructions:
// 1. Define a tuple struct named `Color` with three `u8` fields for RGB.
// 2. Create an instance of `Color` for pure red (255, 0, 0).
// 3. Print each component separately.
//
// Hints:
// - Define a tuple struct with `struct Name(Type1, Type2, Type3);`
// - Access fields with `.0`, `.1`, `.2`.

fn main() {
    // TODO: Create a Color instance and print its components
}
