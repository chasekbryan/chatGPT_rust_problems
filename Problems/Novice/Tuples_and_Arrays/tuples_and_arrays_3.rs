// tuples_and_arrays_3.rs
// Type: Tuples and Arrays
// Difficulty: Novice (Level 1)
//
// Lesson:
// Tuples can be returned from functions. This allows returning multiple values at once.
//
// Instructions:
// 1. Define a function named `swap` that takes a tuple `(i32, i32)`.
// 2. It should return a tuple where the two values are swapped.
// 3. In `main`, call `swap` with `(1, 2)` and print the result.
//
// Hints:
// - Define the parameter type and return type as `(i32, i32)`.
// - Create and return a new tuple inside the function.

fn main() {
    // TODO: Call swap with (1, 2) and print the result
}
