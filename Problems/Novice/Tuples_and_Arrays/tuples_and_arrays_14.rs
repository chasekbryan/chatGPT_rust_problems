// tuples_and_arrays_14.rs
// Type: Tuples and Arrays
// Difficulty: Novice (Level 1)
//
// Lesson:
// Arrays can be mutable if declared with `mut`, allowing their elements to be changed.
//
// Instructions:
// 1. Create a mutable array named `scores` with `[10, 20, 30, 40, 50]`.
// 2. Change the third element (index 2) to `35`.
// 3. Print the updated array.
//
// Hints:
// - Declare with `let mut array = [...]`.
// - Assign a new value using `array[index] = value;`.
// - Print the whole array with `println!("{:?}", array);`.

fn main() {
    // TODO: Create a mutable array, modify it, and print it
}
