// tuples_and_arrays_12.rs
// Type: Tuples and Arrays
// Difficulty: Novice (Level 1)
//
// Lesson:
// You can use slicing to get parts of an array. A slice is a view into a contiguous sequence of elements.
//
// Instructions:
// 1. Create an array named `numbers` with the values `[0, 1, 2, 3, 4, 5]`.
// 2. Create a slice that contains only the elements `[2, 3, 4]`.
// 3. Print the slice.
//
// Hints:
// - A slice is created with `&array[start..end]` where `start` is inclusive and `end` is exclusive.
// - Use `println!("{:?}", slice)` to print slices.

fn main() {
    // TODO: Create a slice from the array and print it
}
