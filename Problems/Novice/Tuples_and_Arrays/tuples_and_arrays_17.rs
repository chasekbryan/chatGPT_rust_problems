// tuples_and_arrays_17.rs
// Type: Tuples and Arrays
// Difficulty: Novice (Level 1)
//
// Lesson:
// You can iterate over slices as well as full arrays using `for` loops.
//
// Instructions:
// 1. Create an array `[2, 4, 6, 8, 10]`.
// 2. Create a slice containing only `[4, 6, 8]`.
// 3. Use a `for` loop to print each element of the slice.
//
// Hints:
// - Slice with `&array[start..end]`.
// - Use `for element in slice.iter()` to loop through a slice.

fn main() {
    // TODO: Create the array, slice it, and print the slice elements
}
