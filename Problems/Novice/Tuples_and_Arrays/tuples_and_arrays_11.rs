// tuples_and_arrays_11.rs
// Type: Tuples and Arrays
// Difficulty: Novice (Level 1)
//
// Lesson:
// Arrays have fixed size, and indexing outside their bounds will cause a panic at runtime.
//
// Instructions:
// 1. Create an array named `values` with the elements `[1, 2, 3]`.
// 2. Try printing the fourth element (index 3) — observe what happens when you run it.
//
// Hints:
// - Arrays in Rust are 0-indexed: the first element is index 0.
// - There is no fourth element here, so this should cause a runtime panic if you actually run the code.
// - Be careful about array indexing!

fn main() {
    // TODO: Try to print the fourth element (index 3) of the array
}
