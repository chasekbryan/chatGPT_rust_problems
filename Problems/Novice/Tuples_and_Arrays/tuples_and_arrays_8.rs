// tuples_and_arrays_8.rs
// Type: Tuples and Arrays
// Difficulty: Novice (Level 1)
//
// Lesson:
// You can destructure tuples inside a `let` statement to split them into parts easily.
//
// Instructions:
// 1. Create a tuple named `color` with three `u8` values representing RGB: `(255, 165, 0)`.
// 2. Destructure the tuple into `red`, `green`, and `blue` variables.
// 3. Print each variable.
//
// Hints:
// - Use `let (r, g, b) = tuple;` to destructure.
// - Use `println!` to print each value.

fn main() {
    // TODO: Create a tuple, destructure it, and print the RGB components
}
