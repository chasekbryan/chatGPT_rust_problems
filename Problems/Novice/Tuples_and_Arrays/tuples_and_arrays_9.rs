// tuples_and_arrays_9.rs
// Type: Tuples and Arrays
// Difficulty: Novice (Level 1)
//
// Lesson:
// You can pass tuples to functions and destructure them inside the function parameters.
//
// Instructions:
// 1. Define a function named `print_point` that takes a tuple `(i32, i32)` as a parameter.
// 2. Destructure the tuple directly in the function's parameters into `x` and `y`.
// 3. Print `x` and `y` separately inside the function.
// 4. In `main`, call `print_point` with `(4, 9)`.
//
// Hints:
// - The function parameter can be written as `(x, y): (i32, i32)`.
// - You can access `x` and `y` directly inside the function after destructuring.

fn main() {
    // TODO: Call print_point with (4, 9)
}
