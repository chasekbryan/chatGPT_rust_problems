// tuples_and_arrays_2.rs
// Type: Tuples and Arrays
// Difficulty: Novice (Level 1)
//
// Lesson:
// You can destructure a tuple into separate variables in Rust for easier access.
//
// Instructions:
// 1. Create a tuple named `coordinates` with two `i32` values (for example, 5 and 10).
// 2. Destructure the tuple into two variables `x` and `y`.
// 3. Print `x` and `y` separately.
//
// Hints:
// - Destructure a tuple like this: `let (a, b) = tuple;`
// - Use `println!` to print each value.

fn main() {
    // TODO: Create a tuple, destructure it, and print the values
}
