// tuples_and_arrays_15.rs
// Type: Tuples and Arrays
// Difficulty: Novice (Level 1)
//
// Lesson:
// You can return slices from functions to provide a view into part of an array without copying it.
//
// Instructions:
// 1. Define a function named `first_two` that takes a reference to an array `[i32; 4]`.
// 2. It should return a slice containing the first two elements.
// 3. In `main`, create an array `[5, 10, 15, 20]`, call `first_two`, and print the slice.
//
// Hints:
// - The function should return a `&[i32]` slice.
// - Use slicing syntax `&array[start..end]` inside the function.

fn main() {
    // TODO: Create the array, call first_two, and print the result
}
