// tuples_and_arrays_16.rs
// Type: Tuples and Arrays
// Difficulty: Novice (Level 1)
//
// Lesson:
// Arrays can be used in pattern matching with `match` to handle specific array shapes.
//
// Instructions:
// 1. Create a function named `describe` that takes a reference to an array `[i32; 3]`.
// 2. Match the array:
//    - If all elements are zero, return "All zeros".
//    - Otherwise, return "Something else".
// 3. In `main`, call `describe` with `[0, 0, 0]` and print the result.
//
// Hints:
// - Use `match` to pattern match arrays like `[0, 0, 0]`.
// - Return `&str` from the function.

fn main() {
    // TODO: Call describe with [0, 0, 0] and print the result
}
