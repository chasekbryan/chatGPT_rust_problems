// loops_11.rs
// Type: Loops
// Difficulty: Novice (Level 1)
//
// Lesson:
// A `loop` runs forever unless you explicitly `break` out of it.
// This is useful when you want the loop to keep running until some condition is satisfied.
//
// Instructions:
// 1. Create a mutable variable `count` starting at 0.
// 2. Start an infinite `loop`.
// 3. Inside the loop:
//    - Print "Looping..." and the current count.
//    - Increment `count` by 1.
//    - Break the loop once `count` reaches 3.
//
// Hints:
// - Use `loop {}` to start the infinite loop.
// - Use an `if` statement inside the loop to decide when to `break`.

fn main() {
    // TODO: Create a mutable variable count = 0
    
    // TODO: Start an infinite loop
    
        // TODO: Print "Looping..." and the current count
        
        // TODO: Increment count
        
        // TODO: Break the loop when count reaches 3
}
