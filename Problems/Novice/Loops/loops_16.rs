// loops_16.rs
// Type: Loops
// Difficulty: Novice (Level 1)
//
// Lesson:
// A `while let` loop runs as long as a pattern matches.
// It's often used with `Option` or `Result` types, but can also be demonstrated with simple patterns.
//
// Instructions:
// 1. Create a mutable `Option<i32>` variable called `number` set to `Some(5)`.
// 2. Use a `while let` loop to extract the value inside `Some`.
// 3. Print the value.
// 4. After printing, set `number` to `None` to exit the loop.
//
// Hints:
// - `while let Some(value) = number {}` is the basic pattern.
// - Inside the loop body, set `number = None;` after printing.

fn main() {
    // TODO: Create a mutable Option<i32> variable set to Some(5)
    
    // TODO: Write a while let loop to match Some(value)
    
        // TODO: Print the value
        
        // TODO: Set number to None
}
