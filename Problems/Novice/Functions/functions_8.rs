// functions_8.rs
// Type: Functions
// Difficulty: Novice (Level 1)
//
// Lesson:
// Functions can return early using the `return` keyword followed by a value. After a `return`, the function immediately exits.
//
// Instructions:
// 1. Define a function named `early_return_example` that takes an `i32` parameter.
// 2. If the number is negative, immediately return 0.
// 3. Otherwise, return the number itself.
// 4. In `main`, call `early_return_example` with -3 and print the result.
//
// Hints:
// - Use `if` inside the function to check the value.
// - Use `return` to exit early with a specific value.

fn main() {
    // TODO: Call early_return_example with -3 and print the result
}
