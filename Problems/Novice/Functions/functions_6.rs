// functions_6.rs
// Type: Functions
// Difficulty: Novice (Level 1)
//
// Lesson:
// Functions can return different types, including `bool`. Use functions to encapsulate simple logic checks.
//
// Instructions:
// 1. Define a function named `is_even` that takes an `i32` parameter and returns `true` if the number is even, `false` otherwise.
// 2. In `main`, call `is_even` with the number 8 and print the result.
//
// Hints:
// - Use the modulo operator `%` to check for evenness (`num % 2 == 0`).
// - Functions that return `bool` can be directly printed.

fn main() {
    // TODO: Call the is_even function with 8 and print the result
}
