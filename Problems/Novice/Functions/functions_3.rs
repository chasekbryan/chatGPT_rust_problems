// functions_3.rs
// Type: Functions
// Difficulty: Novice (Level 1)
//
// Lesson:
// Functions can return values using the `->` syntax followed by the return type. 
// The last expression inside the function (without a semicolon) is the return value.
//
// Instructions:
// 1. Define a function named `add_two` that takes an `i32` parameter and returns an `i32`.
// 2. The function should return the input value plus 2.
// 3. In `main`, call `add_two` with the value 5 and print the result.
//
// Hints:
// - The function signature will look like: `fn function_name(param: type) -> return_type {}`
// - The returned value should not have a semicolon if you want to return it directly.

fn main() {
    // TODO: Call the add_two function with 5 and print the result
}
