// functions_5.rs
// Type: Functions
// Difficulty: Novice (Level 1)
//
// Lesson:
// Functions can call other functions. This helps keep your code organized and reusable.
//
// Instructions:
// 1. Define a function named `square` that takes one `i32` parameter and returns its square.
// 2. Define another function named `square_plus_one` that:
//    - Takes an `i32` parameter,
//    - Calls `square` on the parameter,
//    - Adds 1 to the result,
//    - Returns the new value.
// 3. In `main`, call `square_plus_one` with the number 5 and print the result.
//
// Hints:
// - To call one function inside another, simply use its name with parentheses and arguments.
// - Remember: squaring means multiplying a number by itself.

fn main() {
    // TODO: Call the square_plus_one function with 5 and print the result
}
