// functions_14.rs
// Type: Functions
// Difficulty: Novice (Level 1)
//
// Lesson:
// Functions can return more complex types like tuples. Tuples group multiple values into one return.
//
// Instructions:
// 1. Define a function named `split_number` that takes an `i32`.
// 2. It should return a tuple containing two `i32` values:
//    - The input number divided by 2
//    - The input number modulo 2
// 3. In `main`, call `split_number` with 9 and print the result.
//
// Hints:
// - Tuples are written like `(value1, value2)`.
// - Use `.0` and `.1` to access tuple elements, or use destructuring with `let (x, y) = ...`.

fn main() {
    // TODO: Call split_number with 9 and print the result
}
