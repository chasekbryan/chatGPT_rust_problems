// functions_18.rs
// Type: Functions
// Difficulty: Novice (Level 1)
//
// Lesson:
// Functions can return different types by using enums or other structures, but for now, we'll use `Option` 
// to represent a value that might or might not exist.
//
// Instructions:
// 1. Define a function named `maybe_divide` that takes two `i32` parameters: numerator and denominator.
// 2. If the denominator is zero, return `None`.
// 3. Otherwise, return `Some(result)` where `result` is the numerator divided by denominator.
// 4. In `main`, call `maybe_divide` with 10 and 2 and print the result.
//
// Hints:
// - `Option` is an enum: `Some(value)` or `None`.
// - Use `if` to check if denominator is zero.

fn main() {
    // TODO: Call maybe_divide with 10 and 2 and print the result
}
