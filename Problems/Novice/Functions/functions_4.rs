// functions_4.rs
// Type: Functions
// Difficulty: Novice (Level 1)
//
// Lesson:
// Functions can have multiple parameters. Separate each parameter with a comma and specify their types individually.
//
// Instructions:
// 1. Define a function named `multiply` that takes two `i32` parameters.
// 2. The function should return the product of the two numbers.
// 3. In `main`, call `multiply` with the numbers 3 and 4, and print the result.
//
// Hints:
// - Separate parameters like this: `fn function_name(param1: type, param2: type) {}`
// - The `*` operator is used for multiplication.

fn main() {
    // TODO: Call the multiply function with 3 and 4, then print the result
}
