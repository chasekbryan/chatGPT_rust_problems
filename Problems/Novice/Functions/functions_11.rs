// functions_11.rs
// Type: Functions
// Difficulty: Novice (Level 1)
//
// Lesson:
// Functions can return different types based on logic branches. In simple cases, all branches must return the same type.
//
// Instructions:
// 1. Define a function named `min` that takes two `i32` parameters.
// 2. It should return the smaller of the two numbers.
// 3. In `main`, call `min` with 8 and 3 and print the result.
//
// Hints:
// - Use an `if` expression to compare the two numbers.
// - Remember that Rust expects both branches of an `if` expression to produce the same type.

fn main() {
    // TODO: Call min with 8 and 3 and print the result
}
