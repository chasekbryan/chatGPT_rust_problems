// functions_9.rs
// Type: Functions
// Difficulty: Novice (Level 1)
//
// Lesson:
// Functions can return different values based on logic. Rust functions use expressions to decide what to return.
//
// Instructions:
// 1. Define a function named `sign` that takes an `i32` parameter.
// 2. Return:
//    - 1 if the number is positive,
//    - 0 if the number is zero,
//    - -1 if the number is negative.
// 3. In `main`, call `sign` with -10 and print the result.
//
// Hints:
// - You can use `if`, `else if`, and `else` expressions to decide what to return.
// - Remember that in Rust, the last evaluated expression (without a semicolon) becomes the return value.

fn main() {
    // TODO: Call the sign function with -10 and print the result
}
