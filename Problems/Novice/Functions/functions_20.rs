// functions_20.rs
// Type: Functions
// Difficulty: Novice (Level 1)
//
// Lesson:
// Functions can be recursive — meaning a function can call itself. 
// Recursive functions must have a base case to stop, or they will cause a stack overflow.
//
// Instructions:
// 1. Define a function named `factorial` that takes a `u32` parameter.
// 2. If the number is 0, return 1 (base case).
// 3. Otherwise, return the number multiplied by `factorial(number - 1)` (recursive case).
// 4. In `main`, call `factorial` with 5 and print the result.
//
// Hints:
// - Use `if` to check if the number is 0.
// - Remember to call the function recursively with `number - 1`.

fn main() {
    // TODO: Call factorial with 5 and print the result
}
