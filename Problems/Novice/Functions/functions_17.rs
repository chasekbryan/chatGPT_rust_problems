// functions_17.rs
// Type: Functions
// Difficulty: Novice (Level 1)
//
// Lesson:
// Functions can return references, but they must follow Rust's borrowing rules. 
// For now, we'll focus on functions that simply return owned data.
//
// Instructions:
// 1. Define a function named `create_greeting` that takes a `String` parameter.
// 2. It should return a new `String` that says "Hello, {name}!" where `{name}` is the parameter.
// 3. In `main`, call `create_greeting` with your name and print the result.
//
// Hints:
// - You can use the `format!` macro to build a new `String`.
// - Example: `format!("Hello, {}!", name)`

fn main() {
    // TODO: Call create_greeting with your name and print the result
}
