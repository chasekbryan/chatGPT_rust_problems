// functions_2.rs
// Type: Functions
// Difficulty: Novice (Level 1)
//
// Lesson:
// Functions can accept parameters to operate with different inputs. Parameters are specified inside the parentheses 
// when defining the function. You must also specify the type of each parameter.
//
// Instructions:
// 1. Define a function named `print_number` that takes one parameter named `num` of type `i32`.
// 2. The function should print "The number is: X", where X is the value of `num`.
// 3. In `main`, call `print_number` with the value 7.
//
// Hints:
// - Define the parameter like this: `fn function_name(parameter_name: type) {}`
// - Inside `println!`, use `{}` to print the value of `num`.

fn main() {
    // TODO: Call the print_number function here with 7
}
