// functions_15.rs
// Type: Functions
// Difficulty: Novice (Level 1)
//
// Lesson:
// Functions can take ownership of parameters or borrow them. For now, we practice passing ownership with `String`.
//
// Instructions:
// 1. Define a function named `print_and_consume` that takes a `String` parameter.
// 2. It should print the string to the console.
// 3. In `main`, create a `String` and pass it to `print_and_consume`.
//
// Hints:
// - When a `String` is passed to a function without borrowing (`&`), ownership moves into the function.
// - After moving, you cannot use the original variable again unless you clone it before passing.

fn main() {
    // TODO: Create a String and pass it to print_and_consume
}
