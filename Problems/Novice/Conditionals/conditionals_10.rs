// conditionals_10.rs
// Type: Conditionals
// Difficulty: Novice (Level 1)
//
// Lesson:
// Rust's if-let statement provides concise syntax for handling optional values or pattern matching on enums.
//
// Instructions:
// 1. Declare an `Option<i32>` variable named `number` that can either hold `Some(value)` or `None`.
// 2. Use an `if let` statement to check if the option contains a value:
//    - If there is a value, print "The number is: {value}".
//    - If the option is `None`, do nothing.
//
// Hints:
// - Use the syntax `if let Some(value) = option` to extract the value if present.
// - The `if let` construct simplifies handling optional types or enums without exhaustive matching.

fn main() {
  // TODO: Declare `Option<i32>` variable named `number`.

  // TODO: Write an `if let` statement to handle `Some(value)`.
}
