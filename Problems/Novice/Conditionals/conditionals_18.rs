// conditionals_18.rs
// Type: Conditionals
// Difficulty: Novice (Level 1)
//
// Lesson:
// Matching with structs allows you to destructure fields and use them in logic.
//
// Instructions:
// 1. Define a struct `Book` with fields `title` (String) and `pages` (u32).
// 2. Create a variable `my_book` with a `Book` instance.
// 3. Use a match statement to:
//    - If the book has more than 300 pages, print "Long read: {title}".
//    - Otherwise, print "Quick read: {title}".
//
// Hints:
// - Match the `Book` struct with syntax like `Book { title, pages }`.
// - You can use guards inside match arms (e.g., `if pages > 300`).

fn main() {
  // TODO: Define the `Book` struct.

  // TODO: Create a `my_book` instance.

  // TODO: Match on the struct and apply a condition.
}
