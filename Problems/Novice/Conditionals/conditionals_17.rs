// conditionals_17.rs
// Type: Conditionals
// Difficulty: Novice (Level 1)
//
// Lesson:
// The `matches!` macro can be used for simple pattern checking without writing a full match statement.
//
// Instructions:
// 1. Define an enum `Pet` with variants `Dog`, `Cat`, and `Bird`.
// 2. Declare a variable `my_pet` of type `Pet`.
// 3. Use the `matches!` macro to check if `my_pet` is a `Dog`, and print "You have a dog!" if true.
// 4. Otherwise, print "You have another pet.".
//
// Hints:
// - `matches!(value, pattern)` returns a boolean.
// - You can use it inside an `if` statement.

fn main() {
  // TODO: Define enum `Pet`.

  // TODO: Declare variable `my_pet`.

  // TODO: Use `matches!` macro inside an `if` statement.
}
