// conditionals_11.rs
// Type: Conditionals
// Difficulty: Novice (Level 1)
//
// Lesson:
// The `if let` statement can also include an `else` branch to handle the case when the pattern does not match.
//
// Instructions:
// 1. Declare an `Option<&str>` variable named `favorite_color`.
// 2. Use an `if let` statement:
//    - If there is a favorite color, print "Your favorite color is {color}!".
//    - Otherwise (if `None`), print "You don't have a favorite color.".
//
// Hints:
// - You can attach an `else` block to an `if let` to handle the non-matching case.
// - String slices (`&str`) can be directly printed using `println!`.

fn main() {
  // TODO: Declare `Option<&str>` variable `favorite_color`.

  // TODO: Use `if let` with an `else` block.
}
