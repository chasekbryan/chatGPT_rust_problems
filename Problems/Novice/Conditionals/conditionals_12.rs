// conditionals_12.rs
// Type: Conditionals
// Difficulty: Novice (Level 1)
//
// Lesson:
// A `match` statement can also be used with enums to perform different actions depending on the variant.
//
// Instructions:
// 1. Define an enum `LightState` with three variants: `Red`, `Yellow`, and `Green`.
// 2. Declare a variable named `current_light` of type `LightState`.
// 3. Use a `match` statement to:
//    - Print "Stop!" for `Red`.
//    - Print "Caution!" for `Yellow`.
//    - Print "Go!" for `Green`.
//
// Hints:
// - Enum variants are typically written in CamelCase.
// - Pattern match directly on enum variants without needing to qualify them if they are in scope.

fn main() {
  // TODO: Define enum `LightState`.

  // TODO: Declare variable `current_light` of type `LightState`.

  // TODO: Write a match statement for `current_light`.
}
