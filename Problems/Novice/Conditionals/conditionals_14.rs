// conditionals_14.rs
// Type: Conditionals
// Difficulty: Novice (Level 1)
//
// Lesson:
// You can match against tuples in Rust, allowing you to check combinations of multiple values at once.
//
// Instructions:
// 1. Declare two variables: `x` and `y`, both integers.
// 2. Use a `match` statement on the tuple `(x, y)`:
//    - If both are zero, print "Origin".
//    - If `x` is zero, print "On the Y-axis".
//    - If `y` is zero, print "On the X-axis".
//    - Otherwise, print "Somewhere else".
//
// Hints:
// - Match patterns like `(0, 0)`, `(0, _)`, `(_, 0)`, and `(_, _)`.
// - Use `_` to match any value without binding it.

fn main() {
  // TODO: Declare variables `x` and `y`.

  // TODO: Match against the tuple `(x, y)`.
}
