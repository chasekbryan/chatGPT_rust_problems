// strings_and_slices_16.rs
// Type: Strings and Slices
// Difficulty: Novice (Level 1)
//
// Lesson:
// You can collect characters from a string into a `Vec<char>` using `.chars().collect()`, which can be useful for manipulation.
//
// Instructions:
// 1. Create a `String` named `word` with the value "Rust".
// 2. Collect the characters into a `Vec<char>`.
// 3. Print the vector.
//
// Hints:
// - Use `.chars().collect::<Vec<char>>()`.
// - `println!("{:?}", vec)` will print the vector nicely.

fn main() {
    // TODO: Collect the characters into a vector and print it
}
