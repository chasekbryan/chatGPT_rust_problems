// strings_and_slices_13.rs
// Type: Strings and Slices
// Difficulty: Novice (Level 1)
//
// Lesson:
// Strings can be split into parts using the `.split()` method, which returns an iterator over substrings.
//
// Instructions:
// 1. Create a `String` named `sentence` with the value "apple,banana,cherry".
// 2. Split the string by commas.
// 3. Print each fruit separately using a loop.
//
// Hints:
// - Use `.split(',')` to separate the parts.
// - Use a `for` loop to iterate through the results.

fn main() {
    // TODO: Split the string and print each part
}
