// strings_and_slices_6.rs
// Type: Strings and Slices
// Difficulty: Novice (Level 1)
//
// Lesson:
// You can slice a `String` to get a part of it. A slice gives a view into part of the data without copying.
//
// Instructions:
// 1. Create a `String` named `language` with the value "Rust programming".
// 2. Create a slice that contains just "Rust".
// 3. Print the slice.
//
// Hints:
// - Slicing syntax: `&string[start..end]` where `start` is inclusive and `end` is exclusive.
// - Be careful: `.len()` is in bytes, and non-ASCII characters can affect indexing.

fn main() {
    // TODO: Slice "Rust" out of the String and print it
}
