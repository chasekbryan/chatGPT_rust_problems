// strings_and_slices_7.rs
// Type: Strings and Slices
// Difficulty: Novice (Level 1)
//
// Lesson:
// You can iterate over the characters in a string using `.chars()`, which handles Unicode correctly.
//
// Instructions:
// 1. Create a `String` named `word` with the value "hello".
// 2. Use a `for` loop to iterate over each character and print it.
//
// Hints:
// - Use `for c in string.chars()` to get each character.
// - Print each character inside the loop using `println!`.

fn main() {
    // TODO: Loop over each character in the string and print it
}
