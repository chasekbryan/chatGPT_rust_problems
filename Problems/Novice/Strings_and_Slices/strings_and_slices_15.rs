// strings_and_slices_15.rs
// Type: Strings and Slices
// Difficulty: Novice (Level 1)
//
// Lesson:
// You can replace parts of a string using the `.replace()` method. It returns a new `String` with replacements made.
//
// Instructions:
// 1. Create a `String` named `text` with the value "I like cats.".
// 2. Replace "cats" with "dogs".
// 3. Print the new string.
//
// Hints:
// - Use `.replace("old", "new")` to substitute text.
// - The original string is not modified; `.replace()` returns a new `String`.

fn main() {
    // TODO: Replace "cats" with "dogs" and print the new string
}
