// strings_and_slices_18.rs
// Type: Strings and Slices
// Difficulty: Novice (Level 1)
//
// Lesson:
// You can find the index of a substring inside a string using `.find()`, which returns an `Option<usize>`.
//
// Instructions:
// 1. Create a `String` named `phrase` with the value "Find the word here".
// 2. Find the index of the word "word".
// 3. Print the result.
//
// Hints:
// - `.find("substring")` returns `Some(index)` if found, or `None` if not.
// - Use `match` to handle the `Option<usize>`.

fn main() {
    // TODO: Find the substring and print the index if found
}
