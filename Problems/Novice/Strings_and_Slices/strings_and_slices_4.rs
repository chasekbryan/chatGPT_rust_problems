// strings_and_slices_4.rs
// Type: Strings and Slices
// Difficulty: Novice (Level 1)
//
// Lesson:
// You can concatenate two `String` values or a `String` and a `&str` using the `+` operator.
//
// Instructions:
// 1. Create two variables:
//    - A `String` named `hello` with the value "Hello, "
//    - A string slice `world` with the value "world!"
// 2. Use the `+` operator to concatenate them into a new `String`.
// 3. Print the result.
//
// Hints:
// - The `+` operator requires the left-hand side to be a `String` and the right-hand side to be a `&str`.
// - After using `+`, the left-hand `String` is moved.

fn main() {
    // TODO: Concatenate hello and world and print the result
}
