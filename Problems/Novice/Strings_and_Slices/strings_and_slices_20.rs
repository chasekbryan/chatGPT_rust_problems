// strings_and_slices_20.rs
// Type: Strings and Slices
// Difficulty: Novice (Level 1)
//
// Lesson:
// Strings can be created from numbers by converting them. Use `.to_string()` or `format!` to do this.
//
// Instructions:
// 1. Create an `i32` variable named `number` with the value 42.
// 2. Convert `number` into a `String`.
// 3. Print the `String`.
//
// Hints:
// - Use `number.to_string()` to convert the number.
// - Alternatively, you can use `format!("{}", number)`.

fn main() {
    // TODO: Convert the number to a String and print it
}
