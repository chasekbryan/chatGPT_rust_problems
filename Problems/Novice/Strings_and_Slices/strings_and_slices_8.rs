// strings_and_slices_8.rs
// Type: Strings and Slices
// Difficulty: Novice (Level 1)
//
// Lesson:
// String slices (`&str`) can be passed to functions without taking ownership. This is very efficient for working with large strings.
//
// Instructions:
// 1. Define a function named `print_slice` that takes a `&str` parameter.
// 2. Inside the function, print the slice.
// 3. In `main`, call `print_slice` with "Borrowed string".
//
// Hints:
// - Function signature: `fn function_name(param: &str) {}`
// - Use `println!` inside the function.

fn main() {
    // TODO: Call print_slice with "Borrowed string"
}
