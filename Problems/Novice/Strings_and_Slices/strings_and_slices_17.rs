// strings_and_slices_17.rs
// Type: Strings and Slices
// Difficulty: Novice (Level 1)
//
// Lesson:
// You can compare two strings for equality using `==` in Rust, whether they are `String` or `&str` types.
//
// Instructions:
// 1. Create two variables:
//    - A `String` named `a` with "Rust".
//    - A string slice `b` with "Rust".
// 2. Compare them for equality and print the result.
//
// Hints:
// - Rust allows comparing `String` and `&str` directly with `==`.
// - The result is a `bool` you can print.

fn main() {
    // TODO: Compare the two strings and print the result
}
