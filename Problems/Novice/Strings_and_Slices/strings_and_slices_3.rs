// strings_and_slices_3.rs
// Type: Strings and Slices
// Difficulty: Novice (Level 1)
//
// Lesson:
// Strings can be appended to using the `push_str` method, allowing you to add text to an existing `String`.
//
// Instructions:
// 1. Create a mutable `String` named `message` with the value "Hello".
// 2. Append ", world!" to it using `push_str`.
// 3. Print the final `String`.
//
// Hints:
// - You must declare the `String` as `mut` to modify it.
// - Use `message.push_str("text");` to append.

fn main() {
    // TODO: Create a mutable String, append to it, and print it
}
